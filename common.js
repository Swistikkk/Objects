function reduce(array, func) {

  for(let i = 0; i < array.length; i++) {
    // не знаю, как возвращать значение a;
  }

}

let array = [1,2,3];

consolw.log(reduce(array, (a, b) => { return a + b;})));
